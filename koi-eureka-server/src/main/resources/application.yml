server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

# Actuator
management:
  server:
    port: 9002
  endpoints:
    web:
      exposure:
        include: "*"  #监控所有的接口
